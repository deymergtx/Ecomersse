<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Configuración de Perfil - Ecomerse-Soft</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      background-color: #f5f5f5;
    }
    header {
      background-color: #4CAF50;
      color: #fff;
      padding: 2rem;
      text-align: center;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }
    header h1 {
      font-size: 2.5rem;
    }
    .container {
      max-width: 800px;
      margin-top: 2rem;
    }
    .form-label {
      font-weight: bold;
      color: #4CAF50;
    }
    .btn-success {
      background-color: #4CAF50;
      border: none;
      transition: background-color 0.3s;
    }
    .btn-success:hover {
      background-color: #45A049;
    }
    .card {
      border: none;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
      padding: 2rem;
    }
    footer {
      background-color: #333;
      color: #fff;
      padding: 1rem 0;
      margin-top: 3rem;
    }
    footer a {
      color: #4CAF50;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <h1>Configuración de Perfil</h1>
    <p>Actualiza tu información personal</p>
  </header>

  <!-- Contenido Principal -->
  <div class="container">
    <div class="card">
      <form action="/profile/settings" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label">Nombre Completo</label>
          <input type="text" class="form-control" id="name" name="name" value="<%= user && user.name ? user.name : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Correo Electrónico</label>
          <input type="email" class="form-control" id="email" name="email" value="<%= user && user.email ? user.email : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="phone" class="form-label">Teléfono</label>
          <input type="tel" class="form-control" id="phone" name="phone" value="<%= user && user.phone ? user.phone : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="address" class="form-label">Dirección</label>
          <input type="text" class="form-control" id="address" name="address" value="<%= user && user.address ? user.address : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="city" class="form-label">Ciudad</label>
          <input type="text" class="form-control" id="city" name="city" value="<%= user && user.city ? user.city : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="state" class="form-label">Estado/Departamento</label>
          <input type="text" class="form-control" id="state" name="state" value="<%= user && user.state ? user.state : '' %>" required>
        </div>
        <div class="mb-3">
          <label for="occupation" class="form-label">Ocupación</label>
          <input type="text" class="form-control" id="occupation" name="occupation" value="<%= user && user.occupation ? user.occupation : '' %>" required>
        </div>
        <button type="submit" class="btn btn-success w-100">
          <i class="fas fa-save"></i> Actualizar
        </button>
      </form>
    </div>
  </div>

  <!-- Footer -->
  <footer class="text-center">
    <p>&copy; 2024 Ecomerse-Soft. Todos los derechos reservados. <a href="#">Política de Privacidad</a> | <a href="#">Términos de Servicio</a></p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
