<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalle del Producto - Ecomerse-Soft</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/detalles.css">
</head>

<body class="bg-light">
  <!-- Header -->
  <header>
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <!-- Logo y nombre del sitio -->
        <a class="navbar-brand d-flex align-items-center" href="/panel">
          <img src="/images/logo.webp" alt="Ecomerse-Soft" style="height: 50px; width: auto; margin-right: 10px;">
          <span class="fw-bold">Ecomerse-Soft</span>
        </a>
        <!-- Opciones de usuario alineadas a la derecha con espacio entre ellas -->
        <div class="d-flex">
          <a href="/profile" class="btn btn-outline-success me-3">
            <i class="fas fa-user-circle"></i> Perfil
          </a>
          <a href="/map" class="btn btn-outline-success">
            <i class="fas fa-map-marker-alt"></i> Mapa
          </a>
        </div>
      </div>
    </nav>
  </header>

  <!-- Encabezado del Producto -->
  <header class="text-center py-4">
    <h1>Detalle del Producto</h1>
  </header>

  <!-- Contenedor principal -->
  <div class="container">
    <div class="card">
      <!-- Imagen del producto -->
      <img src="/uploads/<%= product.image %>" class="product-image" alt="<%= product.name %>">
 
      <div class="card-body">
        <!-- Nombre del producto -->
        <h2 class="card-title text-success"><%= product.name %></h2>

        <!-- Precio del producto -->
        <p class="card-text fw-bold fs-4">Precio: $<%= product.price.toFixed(2) %></p>

        <!-- Descripción -->
        <p class="card-text">
          <strong>Descripción:</strong>
          <br>
          <%= product.description %>
        </p>

        <!-- Categoría -->
        <p class="card-text">
          <strong>Categoría:</strong> <%= product.category %>
        </p>

        <!-- Ubicación -->
        <p class="card-text">
          <strong>Ubicación:</strong> <%= product.location.coordinates.join(", ") %>
        </p>
      </div>
    </div>

    <!-- Botón de regreso -->
    <div class="text-center">
      <a href="/panel" class="btn btn-success"><i class="fas fa-arrow-left"></i> Volver al Panel</a>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-light text-center text-muted py-3">
    <p>&copy; 2024 Ecomerse-Soft | Todos los derechos reservados</p>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
